[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
[project]
name = "HadronsJobBuilder"
version = "0.1.0"
dependencies = [
  "langchain",
  "langchain_openai",
  "pathlib"
]
